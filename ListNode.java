// Define the ListNode class as public (if left out then its package-private) so that ListNode can be accessed from other files
public class ListNode {
    //instance variables
    int val;
    ListNode next;
    ListNode(int x) { val = x; }
}